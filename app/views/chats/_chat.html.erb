<div class="chat">
	<%= render @messages %>
<div>

<%= form_for @message, { :controller => 'messages', :action => 'create', :remote => 'true' } do |f| %>
	<%= f.text_field :message %>
	<%= f.submit "Send" %>

<% end %>

<%= link_to "Sign Out", signout_path, :controller => 'sessions', :method => 'delete' %>