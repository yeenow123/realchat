<ul class="chat">

<ul>

<%= form_for @message, { :controller => 'messages', :action => 'create' } do |f| %>
	<%= f.text_field :message %>
	<%= f.submit "Send" %>

<% end %>

<%= link_to "Sign Out", signout_path, :controller => 'sessions', :method => 'delete' %>