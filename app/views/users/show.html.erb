<% if signed_in? %>

	<%= link_to "Start Chat", chats_path, :controller => 'chats', :method => 'post' %>
	<%= link_to "Sign Out", signout_path, :controller => 'sessions', :method => 'delete' %>

<% end %>